name: "Sample CI test"

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  Hello-World:
    runs-on: ubuntu-latest
    steps:
      - name: Greetings
        run: echo "Hello world"

      - name: Print date
        run: date